node_modules/
.next/
.next-build/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env
.env.local
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.env*
# ...but the template must ship: it is the only record of which keys the app
# needs, and `.env*` above was quietly excluding it from the repo entirely.
# It holds placeholders only, never real values.
!.env.example
artifacts/
playwright-report/
test-results/
.superpowers/

# local worker media cache
worker/data/
worker/.venv/

# local run logs
*.log
*.log.old

# python bytecode
__pycache__/
*.pyc
worker/.venv-transcribe/

# Personal Claude Code permissions (deploy allowlist) — not the team's call.
.claude/settings.local.json

# Agent worktrees (isolated workspaces, never committed)
.claude/worktrees/
